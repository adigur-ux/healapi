<!DOCTYPE html>
<html>
  <head>
    <title>Zapier Test</title>
  </head>
  <body>
    <h1>Hello Zapier 👋</h1>
    <button id="send">Send to Zap</button>
    <pre id="out"></pre>

    <script>
      const HOOK_URL = "https://hooks.zapier.com/hooks/catch/XXXX/YYYY"; // כתובת ה-WebHook שלך

      document.getElementById("send").onclick = async () => {
        const res = await fetch(HOOK_URL, {
          method: "POST",
          headers: {"Content-Type":"application/json"},
          body: JSON.stringify({ name: "Adi", ts: Date.now() })
        });
        const data = await res.json();
        document.getElementById("out").textContent = JSON.stringify(data,null,2);
      };
    </script>
  </body>
</html>